# Copy to .env.walletapi and align values with the TAuth environment
# HTTP bind address for the wallet API service
WALLETAPI_LISTEN_ADDR=:9090
# Ledger gRPC endpoint; Compose service name works inside docker
WALLETAPI_LEDGER_ADDR=ledger:7000
# Set true when dialing an insecure ledger endpoint (local/dev only)
WALLETAPI_LEDGER_INSECURE=true
# Timeout applied to every ledger RPC (e.g., 3s, 5s)
WALLETAPI_LEDGER_TIMEOUT=3s
# Comma-separated list of allowed browser origins
WALLETAPI_ALLOWED_ORIGINS=http://localhost:8000
# Must match APP_JWT_SIGNING_KEY from TAuth
WALLETAPI_JWT_SIGNING_KEY=replace-with-tauth-signing-key
# Expected JWT issuer (default "tauth")
WALLETAPI_JWT_ISSUER=tauth
# Cookie name TAuth uses for the browser session
WALLETAPI_JWT_COOKIE_NAME=app_session
# Base URL for TAuth, used in response metadata
WALLETAPI_TAUTH_BASE_URL=http://tauth:8080
