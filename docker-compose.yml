services:
  creditsvc:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.creditsvc
    volumes:
      - ledgerdata:/srv/data
    ports:
      - "7000:7000"

volumes:
  ledgerdata: {}
